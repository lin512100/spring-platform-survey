spring:
  application:
    name: spring--platform-cloud-hystrix-dashboard
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/
feign:
  hystrix:
    enabled: true
server:
  port: 2000
management:
  endpoints:
    web:
      exposure:
        include: "*"