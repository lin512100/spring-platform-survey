<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jtang.user.mapper.PlatformMenuMapper">

    <select id="getMenuByUserId" resultType="com.jtang.common.model.oauth.entity.PlatformMenu" parameterType="long">
        SELECT
            *
        FROM
            platform_menu
        WHERE
            id IN (
            SELECT
                menu_id
            FROM
                platform_permission
            WHERE
                role_id IN ( SELECT role_id FROM platform_user_role WHERE user_id IN ( SELECT id FROM platform_user WHERE id = ${userId} ) )
            )
        ORDER BY
            id;
    </select>

</mapper>
