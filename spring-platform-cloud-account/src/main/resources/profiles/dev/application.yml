server:
  port: 3000
spring:
  application:
    name: platform-account
  profiles:
    active: thymeleaf
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://129.204.1.173:3306/platform-account?useUnicode=true&serverTimezone=UTC&useSSL=false&characterEncoding=UTF-8
    username: root
    password: Jtang!14010025
    hikari:
      minimum-idle: 5
      idle-timeout: 180000
      maximum-pool-size: 10
      auto-commit: true
      pool-name: MyHikariCP
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1

  redis:
    host: 129.204.1.173
    port: 6379
    password: Jtang!14010025


mybatis-plus:
  mapper-locations: classpath:/mapper/*/*Mapper.xml
  global-config:
    banner: false
    db-config:
      id-type: AUTO
      table-underline: true
  configuration:
    map-underscore-to-camel-case: true
  type-aliases-package: com.jtang.oauth.controller.entity

eureka:
  client:
    service-url:
      defaultZone: http://129.204.1.173:8761/eureka/
